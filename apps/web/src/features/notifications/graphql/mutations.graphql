mutation MarkNotificationAsRead($id: UUID!) {
    markNotificationAsRead(id: $id) {
        ...NotificationBase
    }
}

mutation MarkAllNotificationsAsRead($workspaceId: UUID!) {
    markAllNotificationsAsRead(workspaceId: $workspaceId)
}
