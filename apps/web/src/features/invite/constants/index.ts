export const INVITE_TOKEN_PARAM = "invite";
export const ACCEPT_INVITE_TOKEN_PARAM = "token";

/** Mirrors WORKSPACE_INVITE_EMAIL_MAX_LENGTH on the API. */
export const INVITE_EMAIL_MAX_LENGTH = 100;
